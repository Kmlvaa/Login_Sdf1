@model Login.Models.AccountLoginVM;
@{
    Layout = "_Layout1";
}
<!-- Sign In Start -->
<div class="backgroundImage">
    <img src="~/img/Screenshot 2024-03-28 134245.png"/>
</div>
<div class="container-fluid main">
            <div class="row h-100 align-items-center justify-content-center" style="min-height: 100vh;">
                <div class="col-12 col-sm-8 col-md-6 col-lg-5 col-xl-4 login">
            <div class="bg-secondary rounded p-4 p-sm-5 my-4 mx-3 login">
                       <form asp-controller="Account" asp-action="Login" method="post">
                        <div class="header"><h2>Welcome to Pinterest</h2></div>
                        <div class="form-floating mb-3">
                            <input asp-for="Email" class="form-control" id="floatingInput" placeholder="name@example.com">
                            <label for="floatingInput">Email address</label>
                            <span asp-validation-for="Email" style="color:red"></span>
                        </div>
                        <div class="form-floating mb-4">
                            <input asp-for="Password" class="form-control" id="floatingPassword" placeholder="Password">
                            <label for="floatingPassword">Password</label>
                            <span asp-validation-for="Password" style="color:red"></span>
                        </div>
                        <div class="d-flex align-items-center justify-content-between mb-4">
                            <div class="form-check">
                                <input type="checkbox" class="form-check-input" id="exampleCheck1">
                                <label class="form-check-label" for="exampleCheck1">Check me out</label>
                            </div>
                            <a href="">Forgot Password</a>
                        </div>
                        <button type="submit" class="btn btn-primary py-3 w-100 mb-4">Sign In</button>
                        @if (!string.IsNullOrWhiteSpace(Model?.ErrorMessage))
                        {
                            <span style="color:red">@Model.ErrorMessage</span>
                        }
                        <p class="text-center mb-0">Don't have an Account? <a asp-controller="Account" asp-action="Register">Sign Up</a></p>
                        </form>
                    </div>
                </div>
            </div>
        </div>
<!-- Sign In End -->
   